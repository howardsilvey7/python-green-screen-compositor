# Generated Outputs

This directory contains generated composite images produced by the Python green-screen workflow.

`example-output.bmp` is committed as a representative result so repository viewers can inspect the program's output without first running the project.

Interactive runs write to `output.bmp` by default. Generated output files may be overwritten when the program is run again; this is intentional and supports reproducible regeneration from source.
